services:
  - type: web
    name: legal-eagle
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app_legal/main.py
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: 10000
    plan: free
